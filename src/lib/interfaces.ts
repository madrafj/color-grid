// common type alias or interface

export type HexColor = `#${string}`

export type ColorStatType = {
  r: boolean;
  g: boolean;
  b: boolean;
  s: boolean;
}

export type ColorEl = 'r' | 'g' | 'b' | 's'
